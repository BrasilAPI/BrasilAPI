apiVersion: v1
kind: Service
metadata:
  name: {{ include "brasilapi.fullname" . }}
  labels:
    {{- include "brasilapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "brasilapi.selectorLabels" . | nindent 4 }}
